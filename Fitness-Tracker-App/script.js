const state=JSON.parse(localStorage.getItem('fitness')||'{}');function render(){steps.textContent=state.steps||0;workouts.textContent=state.workouts||0;calories.textContent=state.calories||0;progress.style.width=Math.min((state.steps||0)/10000*100,100)+'%';message.textContent=state.date?'Last saved: '+state.date:'No activity saved yet.'}function save(){state.steps=Number(sv.value)||0;state.workouts=Number(wv.value)||0;state.calories=Number(cv.value)||0;state.date=new Date().toLocaleDateString();localStorage.setItem('fitness',JSON.stringify(state));render()}render();