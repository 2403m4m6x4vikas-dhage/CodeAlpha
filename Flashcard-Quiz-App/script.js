let cards=[{q:'What is HTML?',a:'HTML is the standard markup language used to structure web pages.'},{q:'What is JavaScript?',a:'JavaScript is a programming language used to add behavior and interactivity to web pages.'},{q:'What is CSS?',a:'CSS is used to style and design HTML elements.'}];let i=0;function render(){question.textContent=cards[i].q;answer.textContent=cards[i].a;answer.classList.add('hidden');count.textContent=`${i+1} / ${cards.length}`}function showAnswer(){answer.classList.remove('hidden')}function next(){i=(i+1)%cards.length;render()}function prev(){i=(i-1+cards.length)%cards.length;render()}function addCard(){let qv=q.value.trim(),av=a.value.trim();if(!qv||!av)return;cards.push({q:qv,a:av});q.value='';a.value='';i=cards.length-1;render()}render();